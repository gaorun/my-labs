# 面向后端的报告侧重

确定受众为后端开发时，按以下结构组织报告内容：

## 接口调用详情
- `xx` 页面在 `yy` 时机调用 `zz` 接口

## 入参来源完整说明
每个参数从哪来（URL 参数、上一接口返回、用户输入框、固定值）

## 出参用途
每个字段怎么展示（表格列、表单默认值、判断条件）

## 前后端职责划分
- 纯前端改动项：文案调整、样式优化、纯前端逻辑变更
- 需后端配合项：新增字段、枚举扩展、状态码语义变更、接口响应结构调整
